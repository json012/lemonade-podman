[project]
name = "lemonade-load-test"
version = "0.1.0"
description = "Load testing script for LiteLLM models to identify lockup conditions"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.108.0",
    "python-dotenv>=1.0.1",
]
